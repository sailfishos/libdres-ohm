SUBDIRS        = src include test # ohm-plugin 

pkgconfigdir   = ${libdir}/pkgconfig
pkgconfig_DATA = src/libdres.pc

MAINTAINTERCLEANFILES = Makefile.in

clean-local:
	find . -name \*~ -exec rm -f {} \;

